# Configuration pour les tests avec Docker
MONGODB_TEST_URI=mongodb://localhost:27017/abc-cours-test
NODE_ENV=test

# Variables pour JWT dans les tests
JWT_SECRET=test-secret-key-for-docker-tests
JWT_EXPIRE=1h

# Configuration pour les tests
TEST_TIMEOUT=30000